# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'hiragana-translation' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for hiragana-translation

  target 'hiragana-translationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'hiragana-translationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'SwiftyJSON', '~> 4.2'
  pod 'Alamofire'
end
